

export default function Profile1({ children }) {
    return (
        <div style={{border:'4px solid gray'}}>
        <p>children as a props in profile1 component</p>
            {children}
        </div>
    )
}
